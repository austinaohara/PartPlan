package edu.farmingdale.partplan.util;

public class ValidationUtils {
    // TODO (Person 4): isNumeric(String input) — return true if parseable as double
    // TODO (Person 4): isPassFail(String input) — return true if input is "PASS" or "FAIL" (case-insensitive)
    // TODO (Person 4): isNotEmpty(String input) — return true if not null and not blank
}

