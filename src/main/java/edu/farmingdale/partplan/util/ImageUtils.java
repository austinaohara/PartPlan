package edu.farmingdale.partplan.util;

public class ImageUtils {
    // TODO (Person 3): loadImage(File file) — return a JavaFX Image from a file path
    // TODO (Person 3): scaleToFit(Image image, double maxWidth, double maxHeight) — scale while keeping aspect ratio
}

