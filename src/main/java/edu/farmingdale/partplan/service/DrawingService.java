package edu.farmingdale.partplan.service;

public class DrawingService {
    // TODO (Person 3): loadFromPDF(File file) — use PDFLoader to render first page as an Image
    // TODO (Person 3): loadFromImage(File file) — use ImageUtils to load image onto canvas
    // TODO (Person 5): saveDrawing(Drawing drawing) — call FirebaseService.saveDrawing()
    // TODO (Person 5): fetchAllDrawings() — call FirebaseService.getAllDrawings(), return list
    // TODO (Person 5): fetchDrawingById(String id) — call FirebaseService.getDrawing(id)
}

