package edu.farmingdale.partplan.service;

public class MeasurementService {
    // TODO (Person 4): checkTolerance(Bubble bubble, double measured) — return true if within range
    // TODO (Person 4): getResultLabel(Bubble bubble, String rawInput) — return "PASS" or "FAIL"
    // TODO (Person 4): computeDeviation(Bubble bubble, double measured) — return measured - nominal
    // TODO (Person 4): isValidInput(Bubble bubble, String input) — true if parseable (NUM) or PASS/FAIL (P/F)
    // TODO (Person 4): computeSummary(List<Measurement>) — return count of total, passed, failed
}
