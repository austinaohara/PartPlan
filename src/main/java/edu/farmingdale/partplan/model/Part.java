package edu.farmingdale.partplan.model;

public class Part {
    // TODO (Person 4): Add fields: partId, drawingId, unitNumber, raceNumber, inspectionDate, inspectedBy, measurements (Map<Integer, String>)
    // TODO (Person 4): Add getters, setters, constructor, toMap(), fromMap()
    // TODO (Person 4): Add computeOverallResult() — returns "PASS" only if all specs pass
    // TODO (Person 5): Confirm field names match Firestore structure
}

