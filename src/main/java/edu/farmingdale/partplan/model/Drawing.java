package edu.farmingdale.partplan.model;

public class Drawing {
    // TODO (Person 3): Add fields: drawingId, drawingName, partNumber, revisionNumber, description, drawingNumber, fileBase64, bubbles (List<Bubble>), createdAt
    // TODO (Person 3): Add getters, setters, constructor, toMap(), fromMap()
    // TODO (Person 5): Confirm field names match exactly what's stored in Firestore
}
