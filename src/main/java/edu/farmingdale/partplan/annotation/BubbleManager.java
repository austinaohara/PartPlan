package edu.farmingdale.partplan.annotation;

public class BubbleManager {
    // TODO (Person 3): Maintain the list of all BubbleNodes currently on the canvas
    // TODO (Person 3): addBubble(x, y) — create a new Bubble model + BubbleNode, add to canvas and list
    // TODO (Person 3): removeBubble(BubbleNode) — remove from canvas and list, renumber remaining bubbles
    // TODO (Person 3): getSelectedBubble() — return the currently selected BubbleNode
    // TODO (Person 3): clearAll() — remove all bubbles from canvas and list
    // TODO (Person 3): getAllBubbles() — return List<Bubble> for saving
}
