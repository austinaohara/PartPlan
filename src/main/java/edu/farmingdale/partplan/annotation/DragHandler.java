package edu.farmingdale.partplan.annotation;

public class DragHandler {
    // TODO (Person 3): Attach to a BubbleNode to enable drag-to-reposition
    // TODO (Person 3): On mouse pressed: record offset between mouse position and node position
    // TODO (Person 3): On mouse dragged: update node's layoutX and layoutY using offset
    // TODO (Person 3): Clamp drag so bubble can't be moved outside the canvas bounds
}

